# 北京精恒光辉门窗有限公司网站部署说明

## 技术栈概述

### 前端
- **HTML/CSS/JavaScript** - 静态页面
- **Apple风格设计** - 简洁大气的界面
- **响应式布局** - 适配各种设备

### 后端
- **Node.js + Express** - 服务器框架
- **SQLite** - 轻量级数据库
- **JWT** - 用户认证
- **CORS** - 跨域支持

## 部署步骤

### 1. 环境准备
确保系统已安装 Node.js (版本 14 或更高)

### 2. 安装依赖
```bash
npm install
```

### 3. 启动服务器
```bash
npm start
```
或者开发模式（自动重启）：
```bash
npm run dev
```

### 4. 访问网站
- **前台网站**: http://localhost:3000
- **后台登录**: http://localhost:3000/admin-login-new.html
- **API健康检查**: http://localhost:3000/api/health

## 管理员账号
- **用户名**: `admin`
- **密码**: `jingheng2024`

## 文件结构
```
网站代码/
├── index.html              # 前台主页面
├── script.js               # 前台JavaScript
├── styles.css              # 前台样式
├── api-client.js           # API客户端
├── admin-login-new.html    # 后台登录页面
├── admin-new.html          # 后台管理页面
├── server.js               # 后端服务器
├── database.js             # 数据库操作
├── package.json            # 项目配置
├── jingheng.db             # SQLite数据库（自动生成）
└── 部署说明.md             # 本文件
```

## API接口

### 公开接口
- `POST /api/customers` - 提交客户咨询
- `POST /api/admin/login` - 管理员登录

### 需要认证的接口
- `GET /api/customers` - 获取客户列表
- `DELETE /api/customers/:id` - 删除客户
- `GET /api/stats` - 获取统计信息

## 数据库说明

### 客户表 (customers)
- `id` - 主键，自增
- `name` - 客户姓名
- `phone` - 联系电话
- `email` - 电子邮箱
- `type` - 咨询类型
- `message` - 咨询内容
- `timestamp` - 提交时间
- `status` - 状态（1=正常，0=删除）

### 管理员表 (admin)
- `id` - 主键，自增
- `username` - 用户名
- `password` - 加密密码
- `created_at` - 创建时间

## 生产环境部署建议

### 1. 使用PM2管理进程
```bash
npm install -g pm2
pm2 start server.js --name "jingheng-website"
pm2 startup
pm2 save
```

### 2. 配置反向代理（Nginx）
```nginx
server {
    listen 80;
    server_name your-domain.com;

    location / {
        proxy_pass http://localhost:3000;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection 'upgrade';
        proxy_set_header Host $host;
        proxy_cache_bypass $http_upgrade;
    }
}
```

### 3. 安全配置
- 修改默认管理员密码
- 配置HTTPS证书
- 设置防火墙规则
- 定期备份数据库

## 故障排除

### 常见问题

1. **端口被占用**
   ```bash
   # 查找占用端口的进程
   netstat -ano | findstr :3000
   # 杀死进程
   taskkill /PID <PID> /F
   ```

2. **数据库连接失败**
   - 检查文件权限
   - 确保磁盘空间充足

3. **前端无法连接API**
   - 检查CORS配置
   - 验证服务器是否运行
   - 检查防火墙设置

### 日志查看
服务器日志会输出到控制台，包含：
- 数据库连接状态
- API请求记录
- 错误信息

## 数据备份
数据库文件 `jingheng.db` 位于项目根目录，建议定期备份。

## 技术支持
如有问题，请联系系统管理员。